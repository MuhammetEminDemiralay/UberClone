import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {

    },
    confirmBtn: {
        backgroundColor : '#000814',
        padding : 10,
        alignItems : 'center',
        margin : 10
    },
    text : {
        color : '#fff',
        fontSize : 20,
        fontWeight : 'bold'
    }

})